# Clock touch motion must finish even though the foliage keeps pulsing.
- action: screenshot
  name: before
- action: tap
  x: 0.5
  y: 0.63
- action: wait_stable
  quiet_ms: 200
  timeout: 3.0
- action: screenshot
  name: stable
