Issues #170, #199 and #130 — the caller half: make the merge sweep prove it read
all of the pull request, make it stop taking the head commit's word for when
that commit arrived, and make it tell the Codex rule WHICH TREE it is deciding
about rather than merely when.

ONE PATCH FOR ONE WORKFLOW, AND THAT IS A DECISION RATHER THAN A CONVENIENCE.
#199 needed four more edits to `pr-merge-sweep.yml` while #170's four were still
parked, and #130 needed three more while both were. Parking them separately
would have produced three patches against one file, an apply order to get right
by hand, and a `merge-candidate.sh` arity that moves twice — which is the state
T-144 exists to stop growing, not to feed. So they travel together: the live
sweep passes nine arguments today and eleven the moment this lands, with nothing
in between for anybody to land halfway.

AND #130'S EDITS ARE NOT OPTIONAL TO THIS PATCH, THEY ARE LOAD-BEARING FOR IT.
The eight edits as they stood deleted `COMMITTED` in edit 5 while two calls two
hunks below still read `"$COMMITTED"` — and this step runs under `set -u`, so
applying them alone aborted the whole sweep on the first pull request it read
comments for, not merely the Codex condition. Found by applying the patch and
reading the result rather than by reading the patch. Whoever applies this now
gets a tree where nothing refers to `COMMITTED` at all.

WHY THIS IS A FILE AND NOT A COMMIT. Agents here run as `claude[bot]` through
the Claude GitHub App, `ATTADIPA_AGENT_TOKEN` is unset by design, and that
installation token holds no `workflows` permission. Verified on 2026-08-24 by
pushing a one-character change to a scratch branch:

    ! [remote rejected] probe/wf-push-170 -> probe/wf-push-170
      (refusing to allow a GitHub App to create or update workflow
       `.github/workflows/pr-merge-sweep.yml` without `workflows` permission)

So the rules, their filters, their query and their tests are on `main`, and the
eleven edits that make the sweep USE them are here. Until this lands the sweep
merges nothing: `merge-candidate.sh` refuses a nine-argument caller by arity and
says so in the log, once per open pull request per sweep, naming this file. That
is the intended state -- a gate that cannot prove it read the whole pull request,
cannot say when its head arrived, and cannot say which tree an answer to the
other reviewer was about, must not merge one -- but it is not the finished one.

A SECOND THING HOLDS UNTIL THIS LANDS, and it holds on a different line. The
live caller passes `"$COMMITTED"` to `codex-answered.sh`, which since #130 was
reopened expects an object id and refuses a timestamp as one. So a pull request
carrying a Codex finding answers `unknown` and holds on that as well. Same
direction, different sentence in the log; both end when this is applied.

TO APPLY, from a session whose token may write workflows:

    git checkout main && git pull
    git apply docs/automation/pending/170-merge-sweep-completeness.patch
    git rm docs/automation/pending/170-merge-sweep-completeness.patch
    #  This file only. `git rm -r docs/automation/pending` would also delete
    #  `75-approval-stall.patch` -- 34 kB parked by #128, on `main` today and
    #  not yet applied -- the directory's README, and #154's patch when it
    #  lands. See T-144.
    bash .github/tests/merge-candidate-test.sh   # 184 assertions, and the last
                                                 # four flip to the applied state

THEN THE FIVE DOCUMENTS THAT DESCRIBE THE PARKED STATE, in the same commit.
This directory's README says a patch carries every edit its own landing forces,
and nothing tests prose: the suite checks that this file is gone, not that the
sentences about it are true. Applying the patch without these leaves five
documents asserting the opposite of the truth, which is the defect
CLAUDE_AUTOMATION.md calls blocking on #154, mirrored. The list was three for
three rounds, and the two it was missing were the README that states the rule
and the file CLAUDE.md sends every agent to first. Before committing, run all
four of `grep -rn 170-merge-sweep-completeness`, `grep -rn 'merges nothing'`,
`grep -rn 'HALF LIVE'` and `grep -rn 'pr-merge-sweep.yml' docs/`. Not one of
them is redundant, and each was added after the list came up short without it:
the conditions-table row above the banner contains neither of the first two
strings; `merges nothing` hits an unrelated undrafting row in the same file;
and the README paragraph that says THIS is the one patch editing the sweep
names neither the patch nor any of those phrases -- it says *this one*. Three
greps looked like they had covered the file three rounds running.

The list below is what those four find, checked against the file rather than
remembered. Where a paragraph is named, it is named BY THE WORDS IT OPENS WITH:
two rounds were lost to entries that described a paragraph well enough for a
human and matched a different one.

    CLAUDE.md            the paragraph beginning "The half-hourly sweep merges
                         nothing, from this change until the parked patch
                         lands" -- delete it. The sentences above it, which
                         describe a sweep that merges three per run, become
                         true again the moment this lands.
    docs/automation/CLAUDE_AUTOMATION.md
                         THREE places, and the two in the conditions table are
                         the ones a reader of this list misses, because the
                         banner is the only one that looks like a banner:
                           - the section opening "### (warning) Two protections
                             are half live, and this is which half", and the
                             whole table under it, including "It merges nothing
                             at all" and the row answering "Who can finish
                             it?";
                           - fourteen lines ABOVE that heading, the conditions
                             row opening "**A page is not a set -- (warning)
                             HALF LIVE, see below.**";
                           - two rows below that one, the conditions row
                             opening "**A six-hour settling window, on a clock
                             the author does not hold -- (warning) HALF LIVE,
                             see below.**", which #199 added and which carries
                             the same pointer.
                         Leave either row and a fully live protection still says
                         half, pointing `see below` at a section that is gone.
    STATUS.md            the "Agent automation" row's closing sentence, which
                         names this file as parked.
    docs/automation/pending/README.md
                         THREE things, and each is named here by the words it
                         actually opens with, because two rounds got this wrong
                         by describing them instead:
                           - this patch's row in "Waiting now". The row is the
                             worked example :31-33 gives for the very rule this
                             block quotes, and a row for a file that is gone
                             advertises a patch nobody can apply.
                           - the paragraph opening "For
                             `170-merge-sweep-completeness.patch`:" -- NOT the
                             one opening "Verified before it was parked", which
                             is #128's record about `75-approval-stall.patch`
                             and the only place its verification is written
                             down. Deleting that one is the accident this line
                             exists to prevent.
                           - the paragraph opening "One patch in this directory
                             edits `pr-merge-sweep.yml`" — all of it EXCEPT its
                             closing sentence, "Every patch here `git rm`s only
                             its own file...", which is a directory invariant
                             that outlives this patch and must stay. The rest is
                             unconditional and about this patch. It is not the
                             "Two patches now edit" sentence, which was replaced
                             and no longer exists.
    TASKS.md             T-144 stays open for #154's patch, so it is edited
                         rather than closed. Its Impact says the sweep
                         "merges NOTHING" because the arity refusal holds
                         every pull request -- that ends here: the caller
                         passes eleven arguments the moment this lands. Its
                         "Apply both in one commit" instruction then governs
                         one patch, not two.

    git commit -am "The merge sweep asks whether it read all of the pull request"

WHAT IT CHANGES, all in .github/workflows/pr-merge-sweep.yml. The first four are
#170's, the next four are #199's and the last three are #130's:

  1. the inline GraphQL query becomes `-F query=@.github/scripts/merge-facts.graphql`,
     which is the same query plus `pageInfo` on all six decision-critical
     connections, `labels(first:100)` in place of `first:50`, `oid` and
     `checkSuites` on the head commit, and no `committedDate` or `pushedDate`
     at all;
  2. a completeness step between reading the facts and using them, which holds
     on anything `merge-facts.sh` cannot prove complete;
  3. the `FILE_COUNT >= 100` guard goes, because the schema now answers that
     question exactly for all six connections instead of by length for one;
  4. the verdict call passes the completeness answer as its tenth argument;
  5. the `COMMITTED = (.pushedDate // .committedDate)` extraction goes, and with
     it the hand-rolled `PASS_AT` / `PASS_AFTER_HEAD` comparison built on it;
  6. a head-trust step in their place, `.github/scripts/merge-head-trust.sh`,
     which returns the head's object id, whether the latest `ai-review:pass`
     labelling post-dates GitHub's own record of that head arriving, and how old
     that arrival is -- and holds outright where GitHub has stamped nothing;
  7. the separate `HEAD_AGE` computation goes, because it is one of those three;
  8. the verdict call passes the head's object id as its eleventh argument, and
     the merge notice names the commit it merged;
  9. both calls to `codex-answered.sh` pass `"$HEAD_OID"` in place of
     `"$COMMITTED"` -- which is what makes an acknowledgement have to name the
     tree it is about, and which is also what stops edit 5's deletion from
     leaving two references to a variable `set -u` would abort on;
 10. the inline-comment records carry `commit_oid: (.original_commit_id // null)`,
     the commit a review comment was written against, so a reply in a thread is
     bound to a tree without anybody having to type an object id;
 11. the issue-comment and review-body records carry `commit_oid: null`
     explicitly, because those two shapes have no such field and a record whose
     schema is half-present is one the rule cannot reason about.

Nothing else moves. The path allowlist, the six-hour settling window, the
three-per-run cap, the reviewer's verdict conditions and the CheckRun /
StatusContext separation from a192a89 are untouched.

diff --git a/.github/workflows/pr-merge-sweep.yml b/.github/workflows/pr-merge-sweep.yml
index 7a85cf7..c754218 100644
--- a/.github/workflows/pr-merge-sweep.yml
+++ b/.github/workflows/pr-merge-sweep.yml
@@ -35,6 +35,24 @@ name: Merge what is finished
 # so a hostile contributor's tree cannot execute here -- the only inputs are API
 # facts, and the decision itself is a file that a test can execute:
 # .github/scripts/merge-candidate.sh, .github/tests/merge-candidate-test.sh.
+#
+# SO IS THE QUESTION OF WHETHER THOSE API FACTS ARE ALL OF THEM. A GraphQL
+# connection is a page, and this workflow spent its first weeks treating the
+# first page as the whole set -- so a hundred and first review thread, a hundred
+# and first check and a fifty-first label were all invisible to a gate that
+# could only refuse on what it had read. The query is now
+# .github/scripts/merge-facts.graphql and the completeness rule over its answer
+# is .github/scripts/merge-facts.jq, driven by .github/scripts/merge-facts.sh,
+# for the same reason: a query and a filter inside a YAML block cannot be
+# executed, so nothing can assert what they ask for. Issue #170.
+#
+# AND SO IS THE QUESTION OF WHOSE CLOCK THOSE FACTS ARE ON. The settling window
+# and "was the verdict reached on this head" were both derived here from
+# `(.pushedDate // .committedDate)` -- and `pushedDate` is deprecated and null on
+# every commit this repository has, so both rested on the git committer clock,
+# which `GIT_COMMITTER_DATE` sets. .github/scripts/merge-head-trust.sh replaces
+# it with GitHub's own stamp on the runs it started for that object id, and holds
+# where there is none. Issue #199.
 
 on:
   schedule:
@@ -137,31 +155,14 @@ jobs:
             # GraphQL in one round trip for the facts REST splits across four
             # endpoints, including the two REST cannot answer at all:
             # mergeStateStatus and whether a review thread is resolved.
-            # shellcheck disable=SC2016  # $owner/$name/$number are GraphQL variables, bound by -F below
-            FACTS="$(gh api graphql -f query='
-              query($owner:String!, $name:String!, $number:Int!) {
-                repository(owner:$owner, name:$name) {
-                  pullRequest(number:$number) {
-                    isDraft
-                    mergeStateStatus
-                    labels(first:50) { nodes { name } }
-                    reviewThreads(first:100) { nodes { isResolved } }
-                    files(first:100) { nodes { path } }
-                    timelineItems(last:100, itemTypes:[LABELED_EVENT]) {
-                      nodes { ... on LabeledEvent { createdAt label { name } } }
-                    }
-                    commits(last:1) { nodes { commit {
-                      committedDate
-                      pushedDate
-                      statusCheckRollup { contexts(first:100) { nodes {
-                        __typename
-                        ... on CheckRun { conclusion status }
-                        ... on StatusContext { state }
-                      } } }
-                    } } }
-                  }
-                }
-              }' -F owner="${REPO%%/*}" -F name="${REPO##*/}" -F number="$PR" \
+            #
+            # THE QUERY IS A FILE, .github/scripts/merge-facts.graphql, for the
+            # reason merge-candidate.sh is one: what it asks for is a security
+            # property, and a query inside a YAML block cannot be executed by a
+            # test, so nothing could assert it. What it asks for that this used
+            # to omit is `pageInfo` -- see the step below.
+            FACTS="$(gh api graphql -F query=@.github/scripts/merge-facts.graphql \
+              -F owner="${REPO%%/*}" -F name="${REPO##*/}" -F number="$PR" \
               2>/dev/null)" || FACTS=""
 
             # An empty or error document means the facts were not established.
@@ -172,6 +173,29 @@ jobs:
               continue
             fi
 
+            # DID WE READ ALL OF IT? A GraphQL connection is a PAGE, and until
+            # 2026-08-24 this step asked for `labels(first:50)`,
+            # `reviewThreads(first:100)` and `contexts(first:100)`, never asked
+            # whether there was another page, and computed on what came back as
+            # though it were the whole set. A pull request with 101 review
+            # threads whose hundred-and-first was unresolved reported ZERO
+            # unresolved threads, and zero is the value that merges. `first: N`
+            # had become a silent permitting condition. Issue #170.
+            #
+            # The rule is a file so a test can execute it over real response
+            # shapes; it reads `pageInfo`, never `nodes | length`, and every
+            # unreadable shape holds rather than reading as an empty set.
+            COMPLETE="$(printf '%s' "$FACTS" | bash .github/scripts/merge-facts.sh)"
+            case "$COMPLETE" in
+              COMPLETE) COMPLETE=true ;;
+              HOLD*)
+                echo "::notice::#$PR: ${COMPLETE#HOLD }"
+                continue ;;
+              *)
+                echo "::warning::#$PR: the completeness check answered '$COMPLETE', which is not one of its two answers; leaving it alone"
+                continue ;;
+            esac
+
             IS_DRAFT="$(printf '%s' "$FACTS" | jq -r '.data.repository.pullRequest.isDraft')"
             MERGEABLE="$(printf '%s' "$FACTS" | jq -r '.data.repository.pullRequest.mergeStateStatus // "" | ascii_downcase')"
             # One label per line, not space joined: GitHub permits a space
@@ -179,50 +203,52 @@ jobs:
             # satisfied a space-separated substring test. Found in review.
             LABELS="$(printf '%s' "$FACTS" | jq -r '.data.repository.pullRequest.labels.nodes[].name')"
             UNRESOLVED="$(printf '%s' "$FACTS" | jq -r '[.data.repository.pullRequest.reviewThreads.nodes[] | select(.isResolved | not)] | length')"
-            # pushedDate first, committedDate as the fallback. The settling
-            # window asks when code last ARRIVED, and committedDate is the git
-            # committer clock -- `GIT_COMMITTER_DATE=2020-01-01` defeats it
-            # while pushedDate is observed by GitHub. pushedDate is null for
-            # some commits, which is why the fallback exists and why this is a
-            # narrowing rather than a replacement. Found in review.
-            COMMITTED="$(printf '%s' "$FACTS" | jq -r '
-              .data.repository.pullRequest.commits.nodes[0].commit
-              | (.pushedDate // .committedDate) // "null"')"
-
-            # WHICH COMMIT THE VERDICT WAS REACHED ON. The label says a verdict
-            # happened, not which commit it happened on -- and a review that
-            # reaches no verdict leaves the previous commit's label in place,
-            # which is how a commit nothing reviewed reaches `main`. So the most
-            # recent `labeled ai-review:pass` event is dated against the head.
-            # `unknown` when either date is missing, and unknown holds.
-            PASS_AT="$(printf '%s' "$FACTS" | jq -r '
-              [ .data.repository.pullRequest.timelineItems.nodes[]?
-                | select(.label.name == "ai-review:pass") | .createdAt ]
-              | max // ""')"
-            PASS_AFTER_HEAD=unknown
-            if [ -n "$PASS_AT" ] && [ "$PASS_AT" != "null" ] \
-               && [ -n "$COMMITTED" ] && [ "$COMMITTED" != "null" ]; then
-              PASS_EPOCH=$(date -u -d "$PASS_AT" +%s 2>/dev/null || echo "")
-              HEAD_EPOCH=$(date -u -d "$COMMITTED" +%s 2>/dev/null || echo "")
-              if [ -n "$PASS_EPOCH" ] && [ -n "$HEAD_EPOCH" ]; then
-                if [ "$PASS_EPOCH" -ge "$HEAD_EPOCH" ]; then
-                  PASS_AFTER_HEAD=true
-                else
-                  PASS_AFTER_HEAD=false
-                fi
-              fi
-            fi
+            # WHICH COMMIT IS THIS, WHEN DID GITHUB SEE IT ARRIVE, AND WAS THE
+            # VERDICT REACHED ON IT? Three questions, one answer, and until #199
+            # the answer to all three came out of
+            #
+            #     COMMITTED = (.pushedDate // .committedDate)
+            #
+            # `pushedDate` is deprecated and answers `null` for every commit this
+            # repository has -- read live against the head of #193 on 2026-08-24,
+            # beside a live `committedDate` -- so the fallback was the only
+            # branch ever taken, and the fallback is the git committer clock. A
+            # commit made with `GIT_COMMITTER_DATE` six hours in the past cleared
+            # the settling window at the moment it existed and post-dated no
+            # `ai-review:pass`, so the previous head's verdict read as a verdict
+            # about it. Two security properties, both resting on an input, in a
+            # step whose own comment described the attack and fell back anyway.
+            #
+            # The rule is a file for the reason merge-facts.sh and
+            # merge-candidate.sh are: this normalisation is where BOTH #170 and
+            # #199 lived, and nothing on `main` could execute a line of it. It
+            # reads `workflowRun.createdAt` on the head commit's own check
+            # suites -- GitHub's stamp against that object id -- and holds where
+            # there is none. There is no fallback and adding one is the defect.
+            TRUST="$(printf '%s' "$FACTS" | bash .github/scripts/merge-head-trust.sh)"
+            case "$TRUST" in
+              "TRUSTED "*) : ;;
+              HOLD*)
+                echo "::notice::#$PR: ${TRUST#HOLD }"
+                continue ;;
+              *)
+                echo "::warning::#$PR: the head-trust check answered '$TRUST', which is not one of its two answers; leaving it alone"
+                continue ;;
+            esac
+            # Shape-validated inside the rule above, so these three are known
+            # good before they are read: an object id, one of true/false/unknown,
+            # and whole seconds.
+            IFS=' ' read -r _ HEAD_OID PASS_AFTER_HEAD HEAD_AGE <<<"$TRUST"
 
-            # WHAT IT TOUCHES. `files(first:100)` -- a pull request with more
-            # than a hundred changed files reads as a truncated list, so the
-            # count is compared against `changedFiles` below and a truncation
-            # holds rather than merging on a partial read.
+            # WHAT IT TOUCHES. The truncation guard that used to live here read
+            # `nodes | length >= 100`, which was the right worry answered by the
+            # wrong evidence: it cannot tell an exactly-full page from a
+            # truncated one, so it held a pull request with exactly a hundred
+            # files and, more to the point, it was the only connection of the
+            # five that had a guard at all. The completeness step above now asks
+            # the schema about all five, so a hundred files with
+            # `hasNextPage: false` is a complete list and reads as one.
             PATHS="$(printf '%s' "$FACTS" | jq -r '.data.repository.pullRequest.files.nodes[]?.path')"
-            FILE_COUNT="$(printf '%s' "$FACTS" | jq -r '[.data.repository.pullRequest.files.nodes[]?] | length')"
-            if [ "$FILE_COUNT" -ge 100 ]; then
-              echo "::notice::#$PR: 100 or more changed files, so the path list may be truncated; leaving it alone"
-              continue
-            fi
 
             # A run still in flight has a null conclusion. It must reach the
             # decision as something, and `pending` is what the rule refuses on
@@ -244,10 +270,12 @@ jobs:
                   else "status:" + (.state // "pending") end
                 | ascii_downcase ] | join(" ")')"
 
-            HEAD_AGE=""
-            if [ -n "$COMMITTED" ] && [ "$COMMITTED" != "null" ]; then
-              HEAD_AGE=$(( $(date -u +%s) - $(date -u -d "$COMMITTED" +%s) ))
-            fi
+            # HEAD_AGE used to be computed here, out of `$COMMITTED`, and it is
+            # now one of the three fields the head-trust rule above returns. It
+            # moved rather than being deleted because it is the same question the
+            # settling window has always asked -- "when did code last arrive" --
+            # answered at last by something the author of the commit does not
+            # write. Issue #199.
 
             # The other reviewer. Codex is configured on ChatGPT's side and does
             # not set `ai-review:pass`, so a finding of its reaches us only as a
@@ -267,6 +295,16 @@ jobs:
             # account can leave a comment, and the "answer" needed no relation
             # to the finding beyond a later timestamp. Issue #130.
             #
+            # AND THE THIRD ROUND WAS ABOUT WHICH TREE THE ANSWER WAS ABOUT. The
+            # rule was handed `$COMMITTED` and asked for an answer later than
+            # it, which is the same author-written clock #199 removed from the
+            # settling window two steps above -- and, clock aside, a later
+            # timestamp is not the same tree. Move a pull request onto a commit
+            # that already existed and the head's stamp goes BACKWARDS, so an
+            # acknowledgement written about the previous head clears a finding
+            # on this one. The rule now takes `$HEAD_OID`, an object id nothing
+            # but the tree produces, and an acknowledgement has to name it.
+            #
             # So the facts are gathered here and the rule lives in a file that a
             # test can execute: .github/scripts/codex-answered.sh. Three sources,
             # because a reply relationship only exists in one of them --
@@ -284,6 +322,7 @@ jobs:
               | jq 'if type=="array" and (.[0]|type)=="array" then add else . end
                     | map({kind: "issue", login: .user.login, at: .created_at,
                            bot: (.user.type == "Bot"), thread: null,
+                           commit_oid: null,
                            body: (.body // "")})' \
               2>/dev/null)" || CODEX_ISSUE=""
             CODEX_REVIEW="$(gh api --paginate --slurp \
@@ -292,6 +331,7 @@ jobs:
                     | map(select(.body != "" and .body != null)
                           | {kind: "review", login: .user.login, at: .submitted_at,
                              bot: (.user.type == "Bot"), thread: null,
+                             commit_oid: null,
                              body: (.body // "")})' \
               2>/dev/null)" || CODEX_REVIEW=""
             # `in_reply_to_id // id` is GitHub's own thread root: every comment
@@ -300,11 +340,23 @@ jobs:
             # finding and a reply that this API offers, and it is why inline
             # comments do not need the written acknowledgement that issue
             # comments and review bodies do.
+            #
+            # `original_commit_id` IS WHICH TREE THE THREAD IS ABOUT, and it is
+            # the one field here that answers that question without anybody
+            # typing anything: the commit a review comment was written against,
+            # recorded when it was written. `commit_id` is deliberately not
+            # taken as a fallback -- whether GitHub revises it as a pull request
+            # moves is not something this repository has established, and a
+            # field whose meaning is a guess must not be what binds a verdict to
+            # a tree. Where it is absent the record carries null, no reply can
+            # answer through it, and the written acknowledgement is the way
+            # through. Issue #130.
             CODEX_INLINE="$(gh api --paginate --slurp \
               "repos/$REPO/pulls/$PR/comments?per_page=100" 2>/dev/null \
               | jq 'if type=="array" and (.[0]|type)=="array" then add else . end
                     | map({kind: "review-comment", login: .user.login, at: .created_at,
                            bot: (.user.type == "Bot"), thread: (.in_reply_to_id // .id),
+                           commit_oid: (.original_commit_id // null),
                            body: (.body // "")})' \
               2>/dev/null)" || CODEX_INLINE=""
 
@@ -329,7 +381,7 @@ jobs:
             CODEX=unknown
             if [ -n "$RECORDS" ]; then
               CODEX="$(printf '%s' "$RECORDS" \
-                       | bash .github/scripts/codex-answered.sh "$COMMITTED" 2>/dev/null)" || CODEX=unknown
+                       | bash .github/scripts/codex-answered.sh "$HEAD_OID" 2>/dev/null)" || CODEX=unknown
             fi
 
             if [ "$CODEX" != "0" ] && [ -n "$RECORDS" ]; then
@@ -366,7 +418,7 @@ jobs:
                 '[ $r[] | . + {permission: ($p[.login] // "unknown")} ]' 2>/dev/null)" || RECORDS=""
               if [ -n "$RECORDS" ]; then
                 CODEX="$(printf '%s' "$RECORDS" \
-                         | bash .github/scripts/codex-answered.sh "$COMMITTED" 2>/dev/null)" || CODEX=unknown
+                         | bash .github/scripts/codex-answered.sh "$HEAD_OID" 2>/dev/null)" || CODEX=unknown
               fi
             fi
             case "$CODEX" in
@@ -376,7 +428,8 @@ jobs:
 
             VERDICT="$(bash .github/scripts/merge-candidate.sh \
               "$CHECKS" "$LABELS" "$UNRESOLVED" "$CODEX" \
-              "$MERGEABLE" "$IS_DRAFT" "$HEAD_AGE" "$PATHS" "$PASS_AFTER_HEAD")"
+              "$MERGEABLE" "$IS_DRAFT" "$HEAD_AGE" "$PATHS" "$PASS_AFTER_HEAD" \
+              "$COMPLETE" "$HEAD_OID")"
 
             case "$VERDICT" in
               HOLD*)
@@ -418,7 +471,10 @@ jobs:
 
             if gh pr merge "$PR" --repo "$REPO" --squash --delete-branch; then
               MERGED=$((MERGED + 1))
-              echo "::notice::#$PR: merged"
+              # The commit, not just the number. Everything above was decided
+              # about one object id, and a log line that does not carry it
+              # cannot be checked afterwards against what was reviewed.
+              echo "::notice::#$PR: merged at ${HEAD_OID:0:8}"
             else
               REFUSED=$((REFUSED + 1))
               echo "::warning::#$PR: merge refused by GitHub; nothing changed"
